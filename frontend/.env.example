# =============================================================================
# Frontend .env Example (Development Mode)
# =============================================================================
# 说明：
# 1. 本文件示例仅用于本地开发（Vite dev server）
# 2. 复制为 `.env` 后，根据实际环境替换为真实值
# 3. Docker 构建时请设置对应的 build args（详见 docker-compose.yml）
# =============================================================================

# API 基础路径（开发环境指向后端服务地址）
VITE_API_BASE=http://localhost:8080/api

# 高德地图开放平台 API Key（必填）
# https://lbs.amap.com/
VITE_AMAP_KEY=your-amap-key-here

# 高德地图安全密钥
VITE_AMAP_SECURITY_JS_CODE=your-amap-security-code